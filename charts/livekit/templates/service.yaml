apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name | default (include "livekit.fullname" .) }}
  labels:
    {{- include "livekit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "livekit.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
