LiveKit endpoint:
  wss://{{ .Values.ingress.host }}

Required node firewall ports:
  {{ .Values.livekit.rtc.tcpPort }}/TCP
  {{ .Values.livekit.rtc.udpPort }}/UDP

Service:
  {{ .Values.service.name | default (include "livekit.fullname" .) }}

Kubernetes Service Links:
  {{ .Values.enableServiceLinks }}
